# ── Runtime dependencies ───────────────────────────────────────────────────────
Flask>=3.0.0          # Web framework
python-dotenv>=1.0.0  # Load credentials from .env file
gunicorn>=21.2.0      # Production WSGI server (Railway, Render, VPS)
resend>=2.0.0         # Transactional email via Resend API
